# Sakura Talk - Design System Documentation
## Version 3.0 (Instagram-Inspired)

このドキュメントは、Sakura Talk v3のデザインシステムを包括的に記録したものです。
Instagram [@sakura.talk](https://www.instagram.com/sakura.talk/) のブランドアイデンティティに基づいて設計されています。

---

## 📋 目次

1. [デザインコンセプト](#デザインコンセプト)
2. [カラーパレット](#カラーパレット)
3. [タイポグラフィ](#タイポグラフィ)
4. [スペーシング](#スペーシング)
5. [レイアウト](#レイアウト)
6. [コンポーネント](#コンポーネント)
7. [アニメーション](#アニメーション)
8. [レスポンシブデザイン](#レスポンシブデザイン)

---

## デザインコンセプト

### ブランドの特徴

| 属性 | 説明 |
|------|------|
| **トーン** | 親しみやすく、誠実で、温かみがある |
| **スタイル** | モダンでありながら、日本の伝統的な美意識を取り入れた |
| **雰囲気** | 居心地の良いカフェのような、リラックスした空間 |
| **ターゲット** | 日本に興味がある海外の方、日本への旅行・移住を検討している人 |

### デザイン原則

1. **Authenticity（本物志向）** - 飾らない、リアルな日本の姿を伝える
2. **Warmth（温かさ）** - 視覚的にも心理的にも温かみを感じさせる
3. **Clarity（明瞭さ）** - 情報を分かりやすく、読みやすく提示する
4. **Personal（個人的）** - 大企業ではなく、個人としてのつながりを大切にする

### Instagramからの着想

@sakura.talk のInstagram投稿から抽出した視覚的特徴：

- 🏠 **温かみのある室内照明** → 暖色系の背景色
- 🍵 **抹茶グリーンの店舗看板** → プライマリーカラー
- 🌸 **桜や日本的なモチーフ** → アクセントカラー
- 👤 **自然体で誠実な姿** → 装飾過多を避けたクリーンなデザイン
- 📷 **高品質だが親しみやすい写真** → プロフェッショナルだが堅くない

---

## カラーパレット

### プライマリーカラー（Primary Colors）

日本の自然からインスピレーションを得た色彩。

| 名前 | HEX | RGB | 用途 |
|------|-----|-----|------|
| **Matcha** | `#4A6741` | rgb(74, 103, 65) | メインのアクションカラー、ボタン、リンク |
| **Matcha Light** | `#6B8B62` | rgb(107, 139, 98) | ホバー状態、軽いアクセント |
| **Matcha Dark** | `#3A5234` | rgb(58, 82, 52) | ホバー状態、強調 |

```css
--color-matcha: #4A6741;
--color-matcha-light: #6B8B62;
--color-matcha-dark: #3A5234;
```

### アクセントカラー（Accent Colors）

桜をイメージした柔らかいピンク。

| 名前 | HEX | RGB | 用途 |
|------|-----|-----|------|
| **Sakura** | `#F2B5C4` | rgb(242, 181, 196) | アイコン背景、装飾、ハイライト |
| **Sakura Deep** | `#E896AB` | rgb(232, 150, 171) | ホバー状態 |
| **Sakura Light** | `#FADCE5` | rgb(250, 220, 229) | カード背景、グラデーション |

```css
--color-sakura: #F2B5C4;
--color-sakura-deep: #E896AB;
--color-sakura-light: #FADCE5;
```

### セカンダリーカラー（Secondary Colors）

| 名前 | HEX | RGB | 用途 |
|------|-----|-----|------|
| **Navy** | `#2C3E50` | rgb(44, 62, 80) | 見出し、重要なテキスト |
| **Navy Light** | `#3D5166` | rgb(61, 81, 102) | サブ見出し |
| **Burgundy** | `#8B3A4C` | rgb(139, 58, 76) | エラー、警告 |
| **Gold** | `#D4A574` | rgb(212, 165, 116) | 特別なハイライト |

```css
--color-navy: #2C3E50;
--color-burgundy: #8B3A4C;
--color-gold: #D4A574;
```

### ニュートラルカラー（Neutral Colors）

温かみのあるクリーム系の背景色。

| 名前 | HEX | RGB | 用途 |
|------|-----|-----|------|
| **Cream** | `#FDF8F3` | rgb(253, 248, 243) | メイン背景 |
| **Cream Warm** | `#F9F1E8` | rgb(249, 241, 232) | セクション背景（交互） |
| **Cream Soft** | `#FFF9F4` | rgb(255, 249, 244) | カード背景 |
| **White** | `#FFFFFF` | rgb(255, 255, 255) | カード、コンテンツ背景 |
| **Charcoal** | `#2D2D2D` | rgb(45, 45, 45) | 本文テキスト |
| **Dark Gray** | `#4A4A4A` | rgb(74, 74, 74) | サブテキスト |
| **Warm Gray** | `#7A7067` | rgb(122, 112, 103) | 説明文、補助テキスト |
| **Light Gray** | `#B8B0A5` | rgb(184, 176, 165) | プレースホルダー、無効状態 |

```css
--color-cream: #FDF8F3;
--color-cream-warm: #F9F1E8;
--color-charcoal: #2D2D2D;
--color-warm-gray: #7A7067;
```

### カラーの使用ルール

```
┌─────────────────────────────────────────────┐
│  Header: Cream (95% opacity) + blur         │
├─────────────────────────────────────────────┤
│  Hero: Gradient (Cream → Sakura Light)      │
├─────────────────────────────────────────────┤
│  Section A: Cream Warm                      │
├─────────────────────────────────────────────┤
│  Section B: Cream                           │
├─────────────────────────────────────────────┤
│  CTA: Matcha Gradient                       │
├─────────────────────────────────────────────┤
│  Footer: Navy                               │
└─────────────────────────────────────────────┘
```

---

## タイポグラフィ

### フォントファミリー

| 用途 | フォント | フォールバック |
|------|----------|----------------|
| **見出し** | DM Serif Display | Noto Serif JP, Georgia, serif |
| **本文** | Inter | Noto Sans JP, -apple-system, sans-serif |
| **アクセント** | Quicksand | Inter, sans-serif |

```css
--font-heading: 'DM Serif Display', 'Noto Serif JP', Georgia, serif;
--font-body: 'Inter', 'Noto Sans JP', -apple-system, BlinkMacSystemFont, sans-serif;
--font-accent: 'Quicksand', 'Inter', sans-serif;
```

### フォントサイズ（Fluid Typography）

レスポンシブに対応するため、`clamp()`を使用。

| 名前 | 最小 | 推奨 | 最大 | 用途 |
|------|------|------|------|------|
| `--text-xs` | 0.75rem | 0.7rem + 0.25vw | 0.875rem | 補助テキスト、ラベル |
| `--text-sm` | 0.875rem | 0.8rem + 0.375vw | 1rem | 小さい本文、キャプション |
| `--text-base` | 1rem | 0.9rem + 0.5vw | 1.125rem | 本文 |
| `--text-lg` | 1.125rem | 1rem + 0.625vw | 1.25rem | リード文 |
| `--text-xl` | 1.25rem | 1.1rem + 0.75vw | 1.5rem | 小見出し |
| `--text-2xl` | 1.5rem | 1.3rem + 1vw | 2rem | h4 |
| `--text-3xl` | 2rem | 1.6rem + 2vw | 2.75rem | h3 |
| `--text-4xl` | 2.5rem | 2rem + 2.5vw | 3.5rem | h2 |
| `--text-5xl` | 3rem | 2.5rem + 3vw | 4.5rem | h1 |

### フォントウェイト

| 名前 | 値 | 用途 |
|------|-----|------|
| `--font-light` | 300 | 装飾的なテキスト |
| `--font-normal` | 400 | 本文 |
| `--font-medium` | 500 | 強調したい本文 |
| `--font-semibold` | 600 | ボタン、ラベル |
| `--font-bold` | 700 | 見出し |

### 行間（Line Height）

| 名前 | 値 | 用途 |
|------|-----|------|
| `--leading-none` | 1 | 見出し（大きいサイズ） |
| `--leading-tight` | 1.25 | 見出し |
| `--leading-snug` | 1.375 | 短い段落 |
| `--leading-normal` | 1.5 | 一般的な本文 |
| `--leading-relaxed` | 1.625 | 長い本文（推奨） |
| `--leading-loose` | 2 | 日本語テキスト |

### 文字間隔（Letter Spacing）

| 名前 | 値 | 用途 |
|------|-----|------|
| `--tracking-tight` | -0.025em | 大きい見出し |
| `--tracking-normal` | 0 | 本文 |
| `--tracking-wide` | 0.025em | ボタン |
| `--tracking-wider` | 0.05em | 小さいラベル |
| `--tracking-widest` | 0.1em | セクションラベル |

---

## スペーシング

### スペーシングスケール

8pxを基準とした一貫性のあるスペーシングシステム。

| 名前 | 値 | px換算 | 用途 |
|------|-----|--------|------|
| `--space-1` | 0.25rem | 4px | 最小の間隔 |
| `--space-2` | 0.5rem | 8px | アイコンとテキストの間 |
| `--space-3` | 0.75rem | 12px | 関連要素間 |
| `--space-4` | 1rem | 16px | 段落間、小さいカード内 |
| `--space-5` | 1.25rem | 20px | - |
| `--space-6` | 1.5rem | 24px | セクション内要素間 |
| `--space-8` | 2rem | 32px | カード間、グリッドギャップ |
| `--space-10` | 2.5rem | 40px | - |
| `--space-12` | 3rem | 48px | セクションヘッダー下 |
| `--space-16` | 4rem | 64px | モバイルセクション間 |
| `--space-20` | 5rem | 80px | デスクトップセクション間 |
| `--space-24` | 6rem | 96px | - |
| `--space-32` | 8rem | 128px | 大きなセクション間 |

### スペーシングの使用例

```
Section
├── padding-top: var(--space-20)      // 80px
├── Section Header
│   ├── margin-bottom: var(--space-12) // 48px
│   ├── Label (margin-bottom: var(--space-4))
│   ├── Title (margin-bottom: var(--space-4))
│   └── Subtitle
├── Content Grid
│   ├── gap: var(--space-8)           // 32px
│   └── Card
│       └── padding: var(--space-8)   // 32px
└── padding-bottom: var(--space-20)   // 80px
```

---

## レイアウト

### コンテナ

| 名前 | 最大幅 | 用途 |
|------|--------|------|
| `.container` | 1200px | 標準コンテンツ |
| `.container-narrow` | 800px | FAQ、フォーム |
| `.container-wide` | 1400px | 画像ギャラリー |

```css
--container-max-width: 1200px;
--container-padding: 1.5rem; /* 24px, モバイルは16px */
```

### グリッドシステム

```css
/* 基本グリッド */
.grid-cols-2 { grid-template-columns: repeat(2, 1fr); }
.grid-cols-3 { grid-template-columns: repeat(3, 1fr); }

/* オートフィットグリッド */
.grid-auto-fit {
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
}
```

### 2カラムレイアウト

```css
.two-col {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--space-12);
  align-items: center;
}
```

---

## コンポーネント

### ボタン

#### バリエーション

| クラス | 背景 | テキスト | ボーダー | 用途 |
|--------|------|----------|----------|------|
| `.btn-primary` | Matcha | White | Matcha | 主要アクション |
| `.btn-secondary` | Sakura | Charcoal | Sakura | 二次アクション |
| `.btn-outline` | Transparent | Matcha | Matcha | 控えめなアクション |
| `.btn-white` | White | Matcha | White | 暗い背景上 |

#### サイズ

| クラス | Padding | Font Size |
|--------|---------|-----------|
| `.btn-sm` | 8px 20px | 0.875rem |
| `.btn` (default) | 16px 32px | 1rem |
| `.btn-lg` | 20px 40px | 1.125rem |

#### スタイル詳細

```css
.btn {
  border-radius: 9999px;        /* 完全に丸い角 */
  font-family: var(--font-accent);
  font-weight: 600;
  transition: all 250ms ease;
}

.btn:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-lg);
}
```

### カード

#### Feature Card

```
┌─────────────────────────┐
│         [Icon]          │  ← 80x80px, Sakura gradient背景
│                         │
│      Card Title         │  ← text-xl, Navy
│                         │
│   Description text      │  ← text-base, Warm Gray
│   goes here...          │
└─────────────────────────┘
     padding: 40px
     border-radius: 16px
     background: White
```

#### Topic Card

```
┌─────────────────────────┐
│ │  🗼 Topic Title       │  ← border-left: 4px Sakura
│ │                       │
│ │  Description...       │
└─────────────────────────┘
     padding: 24px
     背景: Sakura Light → White gradient
```

#### Pricing Card

```
┌─────────────────────────┐
│      Plan Name          │
│                         │
│        $45              │  ← text-4xl, Matcha
│       /60 min           │
│                         │
│   Description           │
│                         │
│   ✓ Feature 1           │
│   ✓ Feature 2           │
│   ✓ Feature 3           │
│                         │
│    [Choose Plan]        │
└─────────────────────────┘

.featured カードは:
- border: 2px solid Matcha
- "Most Popular" ラベル（上部）
- transform: scale(1.02)
```

### ナビゲーション

#### ヘッダー

```css
.header {
  height: 80px;
  background: rgba(253, 248, 243, 0.95);
  backdrop-filter: blur(10px);
}

.header.scrolled {
  box-shadow: var(--shadow-md);
}
```

#### ナビリンク

```css
.nav-link {
  position: relative;
}

.nav-link::after {
  /* 下線アニメーション */
  width: 0 → 100% on hover;
  background: Matcha;
}
```

### FAQ アコーディオン

```
┌─────────────────────────────────────┐
│ Question text                    +  │  ← クリックで開閉
├─────────────────────────────────────┤
│                                     │
│ Answer text appears here when       │  ← max-height アニメーション
│ the item is expanded...             │
│                                     │
└─────────────────────────────────────┘
     border-bottom: 1px solid Cream Warm
```

---

## アニメーション

### トランジション

| 名前 | 値 | 用途 |
|------|-----|------|
| `--transition-fast` | 150ms ease | ホバー状態 |
| `--transition-base` | 250ms ease | 一般的なトランジション |
| `--transition-slow` | 350ms ease | フェードイン |
| `--transition-slower` | 500ms ease | 大きな要素の変化 |

### スクロールアニメーション

#### Fade In

```css
.fade-in {
  opacity: 0;
  transform: translateY(20px);
}

.fade-in.visible {
  opacity: 1;
  transform: translateY(0);
}
```

#### Slide In

```css
.slide-in-left {
  opacity: 0;
  transform: translateX(-30px);
}

.slide-in-right {
  opacity: 0;
  transform: translateX(30px);
}
```

#### 遅延クラス

```css
.delay-1 { transition-delay: 100ms; }
.delay-2 { transition-delay: 200ms; }
.delay-3 { transition-delay: 300ms; }
.delay-4 { transition-delay: 400ms; }
.delay-5 { transition-delay: 500ms; }
```

---

## レスポンシブデザイン

### ブレークポイント

| 名前 | 値 | 対象デバイス |
|------|-----|--------------|
| Large | min-width: 1400px | 大画面デスクトップ |
| Desktop | default | デスクトップ（1024px〜1399px） |
| Tablet Landscape | max-width: 1024px | タブレット横向き |
| Tablet | max-width: 768px | タブレット縦向き |
| Mobile | max-width: 480px | スマートフォン |

### 主な変更点

#### Tablet (768px以下)

```css
- ナビゲーション → ハンバーガーメニュー
- グリッド 3列/2列 → 1列
- 2カラムレイアウト → 1カラム
- Hero min-height: 100vh → auto
- セクションパディング: 80px → 64px
```

#### Mobile (480px以下)

```css
- h1: text-5xl → text-3xl
- h2: text-4xl → text-2xl
- ボタン: 横並び → 縦並び（full-width）
- カードパディング: 32px → 24px
- セクションパディング: 64px → 48px
```

### アクセシビリティ対応

```css
/* アニメーション無効 */
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}

/* ハイコントラスト */
@media (prefers-contrast: high) {
  .card {
    border: 2px solid var(--color-charcoal);
  }
}
```

---

## シャドウ

| 名前 | 値 | 用途 |
|------|-----|------|
| `--shadow-sm` | 0 1px 2px rgba(..., 0.05) | 軽微な浮き上がり |
| `--shadow-md` | 0 4px 6px rgba(..., 0.07) | カード、ヘッダー |
| `--shadow-lg` | 0 10px 15px rgba(..., 0.08) | ホバー状態 |
| `--shadow-xl` | 0 20px 25px rgba(..., 0.1) | モーダル |
| `--shadow-soft` | 0 8px 30px rgba(..., 0.08) | 画像 |

---

## 角丸（Border Radius）

| 名前 | 値 | 用途 |
|------|-----|------|
| `--radius-sm` | 6px | 小さいボタン、タグ |
| `--radius-md` | 8px | 入力フィールド |
| `--radius-lg` | 12px | カード |
| `--radius-xl` | 16px | 大きいカード |
| `--radius-2xl` | 24px | 画像、特別なカード |
| `--radius-full` | 9999px | ボタン、アバター |

---

## Z-Index

| 名前 | 値 | 用途 |
|------|-----|------|
| `--z-dropdown` | 100 | ドロップダウンメニュー |
| `--z-sticky` | 200 | スティッキー要素 |
| `--z-fixed` | 300 | ヘッダー |
| `--z-modal-backdrop` | 400 | モーダル背景 |
| `--z-modal` | 500 | モーダル、モバイルメニュー |

---

## ファイル構成

```
v3-instagram/
├── index.html
├── css/
│   ├── variables.css    # CSS変数（このドキュメントの値）
│   ├── base.css         # リセット、タイポグラフィ、ユーティリティ
│   ├── layout.css       # コンテナ、グリッド、スペーシング
│   ├── components.css   # ボタン、カード、ナビ等
│   └── responsive.css   # メディアクエリ
├── js/
│   └── main.js          # インタラクション
└── DESIGN-SYSTEM.md     # このドキュメント
```

---

## 使用フォント（Google Fonts）

```html
<link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=Inter:wght@300;400;500;600;700&family=Noto+Sans+JP:wght@300;400;500;600;700&family=Noto+Serif+JP:wght@400;500;600;700&family=Quicksand:wght@400;500;600;700&display=swap" rel="stylesheet">
```

---

## 更新履歴

| 日付 | バージョン | 変更内容 |
|------|------------|----------|
| 2026-02-05 | 3.0 | Instagram @sakura.talk に基づく新デザイン作成 |

---

*このデザインシステムは、Sakura Talk のブランドアイデンティティを維持しながら、一貫性のあるユーザー体験を提供するために作成されました。*
